-- data-only collection of server definitions, read by native vim.lsp.config/enable
-- (in the advanced profile mason-lspconfig handles enabling; in nvim-min see zi/lsp.lua)
return {
  { "neovim/nvim-lspconfig" },
}
