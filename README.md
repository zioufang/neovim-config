## soft link:  
`ln -s <path_to_this_repo>/neovim-config ~/.config/nvim`

## dependencies
```
pacman -S nodejs npm bat ripgrep
yay -S tflint-bin
pip install flake8 cfn-lint isort yapf neovim pynvim --user
```
